function createEmployeeRecord(arr) {
    let timeInEvents = [];
    let timeOutEvents = [];
    let firstName = arr[0];
    let familyName = arr[1];
    let title = arr[2];
    let payPerHour = arr[3]
}